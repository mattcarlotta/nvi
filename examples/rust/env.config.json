{
    "build": {
        "debug": true,
        "execute": "cargo build --release",
        "files": [".env"],
        "required": ["BASIC_ENV", "QUOTES", "INTERP"]
    },
    "debug": {
        "execute": "cargo run",
        "files": [".env"],
        "required": ["BASIC_ENV", "QUOTES", "INTERP"]
    },
    "release": {
        "execute": "cargo run --release",
        "files": [".env"],
        "required": ["BASIC_ENV", "QUOTES", "INTERP"]
    }
}
