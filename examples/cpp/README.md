# C++ Example

Requirements:
- cmake
- make

First build the binary:
```DOSINI
cmake .
make
```

Example usage:
```DOSINI
# assigning ENVs via nvi.toml
nvi --config release
# assigning ENVs via execute command
nvi -- ./cpp_example
```
