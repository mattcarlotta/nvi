# C++ Example

First build the binary:
```DOSINI
cmake .
make
```

Example usage:
```DOSINI
# assigning ENVs via env.config.json
nvi -c release
# assigning ENVs via execute command
nvi -e ./cpp_example
```
